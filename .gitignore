# dependencies
node_modules
.pnp
.pnp.js

# build
.next
out
build
dist

# generated at build time (also committed for CI predictability — optional)
# public/data is generated by compile-graph; keep ignored if you prefer generate-only
# public/data

# testing
coverage
playwright-report
test-results

# misc
.DS_Store
*.pem

# env — never commit secrets; .env.example stays tracked
.env
.env.*
!.env.example

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
next-env.d.ts
*.tsbuildinfo

# ide
.idea
.vscode
